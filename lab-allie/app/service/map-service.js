'use strict';

require('angular')
.module('ngAdventure')
.factory('mapService', ['$log', function($log) {
  $log.debug('#Map service');
  
  let service = {};
  
  service.mapData = {
    room1
  }
  
  
}])